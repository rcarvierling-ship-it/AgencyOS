'use client'
import { useState } from 'react'

export default function DiscoveryForm(){
 const [city,setCity]=useState(''); const [category,setCategory]=useState(''); const [radius,setRadius]=useState('25'); const [loading,setLoading]=useState(false); const [message,setMessage]=useState('')
 async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage('');try{const r=await fetch('/api/admin/discovery',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({city,category,radius:Number(radius)})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Discovery failed');setMessage(`Discovery complete: ${data.created} new businesses added.`)}catch(err){setMessage(err instanceof Error?err.message:'Discovery failed')}finally{setLoading(false)}}
 return <form onSubmit={submit} className="detail"><div className="split"><label className="field">City / ZIP<input required value={city} onChange={e=>setCity(e.target.value)} placeholder="Louisville, KY" /></label><label className="field">Service category<select required value={category} onChange={e=>setCategory(e.target.value)}><option value="">Choose a category</option><option>HVAC</option><option>Plumbing</option><option>Electrical</option><option>Roofing</option><option>Landscaping</option><option>General contractor</option><option>Auto repair</option><option>Cleaning</option><option>Salon</option><option>Dental</option></select></label></div><div className="actions" style={{marginTop:14}}><label className="field" style={{maxWidth:180}}>Radius (miles)<input type="number" min="1" max="100" value={radius} onChange={e=>setRadius(e.target.value)} /></label><button className="primary" disabled={loading}>{loading?'Searching…':'Find businesses'}</button></div>{message&&<div className="note" style={{marginTop:14}}>{message}</div>}<p className="muted" style={{marginTop:14}}>Search results will only be persisted after the discovery provider returns real business records. No placeholder businesses are created.</p></form>
}
