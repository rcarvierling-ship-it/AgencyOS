import Link from 'next/link'
import { AdminShell, Card, Stat, Pill, EmptyState } from '../AdminShell'
import { getClients } from '../data'
export default async function Clients(){const clients=await getClients(); const active=clients.filter(c=>!['closed','cancelled'].includes(c.status)).length; return <AdminShell active="Clients" title="Clients" subtitle="Real client relationships connected to your business graph."><div className="stats"><Stat label="Clients" value={String(clients.length)}/><Stat label="Active" value={String(active)}/><Stat label="Hosted relationships" value={String(clients.filter(c=>c.hostingMode).length)}/><Stat label="Projects" value={String(clients.reduce((n,c)=>n+(c.projects||0),0))}/></div><Card title="Client portfolio">{clients.length?<table className="table"><thead><tr><th>Client</th><th>Status</th><th>Projects</th><th>Websites</th><th>Hosting</th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><td><Link href={`/admin/clients/${c.slug}`}>{c.name}</Link></td><td><Pill tone="green">{c.status}</Pill></td><td>{c.projects}</td><td>{c.websites}</td><td>{c.hostingMode||'—'}</td></tr>)}</tbody></table>:<EmptyState title="No clients yet" body="Clients are created from real won opportunities. There is no placeholder client data." href="/admin/pipeline" label="Open pipeline"/>}</Card></AdminShell>}
