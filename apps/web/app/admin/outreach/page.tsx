import { AdminShell, Card, Pill, Stat, EmptyState } from '../AdminShell'
import { getOutreach } from '../data'
export default async function Outreach(){const rows=await getOutreach(); return <AdminShell active="Outreach" title="Outreach" subtitle="Real prospect communication, drafts, and outcomes."><div className="stats"><Stat label="Activities" value={String(rows.length)}/><Stat label="Sent" value={String(rows.filter(r=>/sent|email/i.test(r.type+' '+r.title)).length)}/><Stat label="Replies" value="—"/><Stat label="Interested" value="—"/></div><Card title="Outreach activity">{rows.length?<table className="table"><thead><tr><th>Business</th><th>Activity</th><th>Type</th><th>Created</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.name}</b></td><td>{r.title}</td><td><Pill>{r.type}</Pill></td><td>{new Date(r.createdAt).toLocaleString()}</td></tr>)}</tbody></table>:<EmptyState title="No outreach activity" body="Nothing has been sent or logged yet. Outreach will appear here once real activity is recorded." href="/admin/businesses" label="View businesses"/>}</Card></AdminShell>}
