import { AdminShell, Card, Pill, Stat, EmptyState } from '../AdminShell'
import { getWebsites } from '../data'
export default async function Hosting(){const hosts=(await getWebsites()).filter(s=>s.hostingMode); return <AdminShell active="Hosting" title="Hosting" subtitle="Real hosted websites and recurring hosting relationships."><div className="stats"><Stat label="Hosted sites" value={String(hosts.length)}/><Stat label="Hosting relationships" value={String(hosts.length)}/><Stat label="Domains" value={String(hosts.filter(s=>s.domain).length)}/><Stat label="Deployments" value="—"/></div><Card title="Hosting fleet">{hosts.length?<table className="table"><thead><tr><th>Client</th><th>Domain</th><th>Environment</th><th>Hosting mode</th></tr></thead><tbody>{hosts.map(s=><tr key={s.id}><td><b>{s.business}</b></td><td>{s.domain||'—'}</td><td><Pill tone={s.status==='production'||s.status==='live'?'green':'amber'}>{s.status}</Pill></td><td>{s.hostingMode}</td></tr>)}</tbody></table>:<EmptyState title="No hosted sites" body="No production website has a hosting relationship recorded yet." href="/admin/websites" label="View websites"/>}</Card></AdminShell>}
